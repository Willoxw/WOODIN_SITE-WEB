<?php
require_once __DIR__ . '/db_connect.php'; if (session_status() !== PHP_SESSION_ACTIVE) session_start();
if (!empty($_SESSION['admin_id'])) { header('Location: index.php'); exit; }
$error = '';
if ($_SERVER['REQUEST_METHOD'] === 'POST') { $stmt=$pdo->prepare('SELECT * FROM admins WHERE username=?'); $stmt->execute([trim(isset($_POST['username']) ? $_POST['username'] : '')]); $admin=$stmt->fetch(); if ($admin && password_verify(isset($_POST['password']) ? $_POST['password'] : '', $admin['password'])) { session_regenerate_id(true); $_SESSION['admin_id']=$admin['id']; $_SESSION['admin_username']=$admin['username']; header('Location: index.php'); exit; } $error='Identifiants invalides.'; }
?><!doctype html><html lang="fr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Connexion administration | Woodin</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"><style>body{min-height:100vh;display:grid;place-items:center;background:#1a1a1a}.card{max-width:420px;width:100%;border:0;border-radius:0}.brand{color:#f5c518;font:900 2rem Georgia}</style></head><body><main class="card shadow p-4"><div class="brand mb-3">WOODIN</div><h1 class="h3">Administration</h1><p class="text-muted">Connectez-vous pour gérer la boutique.</p><?php if($error): ?><div class="alert alert-danger"><?= htmlspecialchars($error) ?></div><?php endif; ?><form method="post"><label class="form-label">Identifiant</label><input class="form-control mb-3" name="username" required><label class="form-label">Mot de passe</label><input class="form-control mb-3" type="password" name="password" required><button class="btn btn-warning w-100" type="submit">Se connecter</button></form></main></body></html>
